#
# This key allows login from one department Linux computer to another without
# a password. It cannot be used to login from outside the department or from
# a UMD wireless connection.
#
# For greater security, add a passphrase to your key this way:
#   ssh-keygen -pf ~/.ssh/id_astronet
#
Host *.astro.umd.edu
  IdentityFile    ~/.ssh/id_astronet
