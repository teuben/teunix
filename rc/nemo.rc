# standard NEMO in NEMO/nemo or teunix/nemo

for s in ~/NEMO/nemo/nemo_start.sh ~/teunix/nemo/nemo_start.sh; do
    if [ -e $d ]; then
       source $s
       echo "Found $s"
       break
    fi	
done    

